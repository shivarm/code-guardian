{
  "ignoreFiles": [
    "package-lock.json",
    "yarn.lock",
    "pnpm-lock.yaml",
    "dist/**",
    "node_modules/**"
  ],
  "rules": [
    {
      "name": "Simple API key assignment",
      "pattern": "api_key\\s*[=:\\s]\\s*([A-Za-z0-9_\\-]{8,})",
      "flags": "gi"
    }
  ]
}
